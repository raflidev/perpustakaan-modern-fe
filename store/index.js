export const state = () => ({
    user:{}
})

// getters 
export const getters = {
    
}

// actions
export const actions = {
}

// mutations 
export const mutations = {
    addUser(state,user) {
        state.user = {}
        state.user = user
    }
}